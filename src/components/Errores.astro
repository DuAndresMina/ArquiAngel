---
// components/ErroresRemodelacion.astro
import LightBulb from '../../public/Icons/lightbulb-outline.svg'
import Cube from '../../public/Icons/cube-outline.svg' 
import Clock from '../../public/Icons/clock-outline.svg'
import AccountHardHat from '../../public/Icons/account-hard-hat-outline.svg'
import CashMultiple from '../../public/Icons/cash-multiple.svg'
---

<section class="relative py-20 bg-[#f9f9f9] overflow-hidden">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Título Principal -->
    <div class="text-center mb-16">
      <div class="inline-flex items-center gap-4 mb-8 animate-heartbeat">
        <LightBulb class="w-12 h-12 text-primary animate-spark" />
        <h2 class="text-4xl md:text-5xl font-bold text-secondary">
          <span class="font-light">5 ERRORES</span><br>
          <span class="text-primary animate-pulse">COMUNES AL REMODELAR</span><br>
          <span class="font-light">TU HOGAR</span>
        </h2>
      </div>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">Evita estos frecuentes descuidos que comprometen tu inversión</p>
    </div>

    <!-- Lista de Errores -->
    <div class="grid gap-12 md:gap-16">
      <!-- Error 01 -->
      <div class="group flex flex-col md:flex-row items-start gap-8 transition-all hover:-translate-y-2" >
        <div class="relative w-full md:w-1/3">
          <div class="text-[120px] md:text-[180px] font-bold text-primary opacity-20 -mt-8">01</div>
          <div class="absolute top-8 left-6">
            <LightBulb class="w-16 h-16 text-secondary group-hover:text-primary transition-colors animate-bulb" />
          </div>
        </div>
        <div class="w-full md:w-2/3">
          <h3 class="text-2xl md:text-3xl font-bold text-secondary mb-4">IGNORAR LA ILUMINACIÓN Y DISTRIBUCIÓN</h3>
          <p class="text-lg text-gray-600 leading-relaxed">Un diseño bien pensado mejora la funcionalidad y estética del espacio.</p>
          <div class="mt-4 h-1 w-full bg-primary transition-all duration-500 transform origin-left scale-x-0 group-hover:scale-x-100"></div>
        </div>
      </div>

      <!-- Error 02 -->
      <div class="group flex flex-col md:flex-row items-start gap-8 transition-all hover:-translate-y-2" >
        <div class="order-2 md:order-1 w-full md:w-2/3 text-right">
          <h3 class="text-2xl md:text-3xl font-bold text-secondary mb-4">ELEGIR MATERIALES DE BAJA CALIDAD</h3>
          <p class="text-lg text-gray-600 leading-relaxed">Lo barato sale caro. Opta por opciones duraderas.</p>
          <div class="mt-4 h-1 w-full bg-primary transition-all duration-500 transform origin-right scale-x-0 group-hover:scale-x-100"></div>
        </div>
        <div class="order-1 md:order-2 w-full md:w-1/3 relative">
          <div class="text-[120px] md:text-[180px] font-bold text-primary opacity-20 -mt-8 text-right">02</div>
          <div class="absolute top-8 right-6">
            <Cube class="w-16 h-16 text-secondary group-hover:text-primary transition-colors animate-float" />
          </div>
        </div>
      </div>

      <!-- Error 03 -->
      <div class="group flex flex-col md:flex-row items-start gap-8 transition-all hover:-translate-y-2" >
        <div class="w-full md:w-1/3 relative">
          <div class="text-[120px] md:text-[180px] font-bold text-primary opacity-20 -mt-8">03</div>
          <div class="absolute top-8 left-6">
            <Clock class="w-16 h-16 text-secondary group-hover:text-primary transition-colors animate-tick" />
          </div>
        </div>
        <div class="w-full md:w-2/3">
          <h3 class="text-2xl md:text-3xl font-bold text-secondary mb-4">NO PLANIFICAR BIEN LOS TIEMPOS</h3>
          <p class="text-lg text-gray-600 leading-relaxed">Evita retrasos asegurando disponibilidad de materiales y mano de obra.</p>
          <div class="mt-4 h-1 w-full bg-primary transition-all duration-500 transform origin-left scale-x-0 group-hover:scale-x-100"></div>
        </div>
      </div>

      <!-- Error 04 -->
      <div class="group flex flex-col md:flex-row items-start gap-8 transition-all hover:-translate-y-2" >
        <div class="order-2 md:order-1 w-full md:w-2/3 text-right">
          <h3 class="text-2xl md:text-3xl font-bold text-secondary mb-4">NO CONTRATAR EXPERTOS</h3>
          <p class="text-lg text-gray-600 leading-relaxed">Un mal trabajo puede generar más costos en el futuro. Confía en profesionales.</p>
          <div class="mt-4 h-1 w-full bg-primary transition-all duration-500 transform origin-right scale-x-0 group-hover:scale-x-100"></div>
        </div>
        <div class="order-1 md:order-2 w-full md:w-1/3 relative">
          <div class="text-[120px] md:text-[180px] font-bold text-primary opacity-20 -mt-8 text-right">04</div>
          <div class="absolute top-8 right-6">
            <AccountHardHat class="w-16 h-16 text-secondary group-hover:text-primary transition-colors animate-helmet" />
          </div>
        </div>
      </div>

      <!-- Error 05 -->
      <div class="group flex flex-col md:flex-row items-start gap-8 transition-all hover:-translate-y-2" >
        <div class="w-full md:w-1/3 relative">
          <div class="text-[120px] md:text-[180px] font-bold text-primary opacity-20 -mt-8">05</div>
          <div class="absolute top-8 left-6">
            <CashMultiple class="w-16 h-16 text-secondary group-hover:text-primary transition-colors animate-cash" />
          </div>
        </div>
        <div class="w-full md:w-2/3">
          <h3 class="text-2xl md:text-3xl font-bold text-secondary mb-4">NO DEFINIR UN PRESUPUESTO</h3>
          <p class="text-lg text-gray-600 leading-relaxed">Sin un plan financiero claro, los gastos pueden salirse de control.</p>
          <div class="mt-4 h-1 w-full bg-primary transition-all duration-500 transform origin-left scale-x-0 group-hover:scale-x-100"></div>
        </div>
      </div>
    </div>

    <!-- Llamado a la Acción -->
    <div class="mt-20 text-center">
      <div class="bg-secondary p-8 rounded-2xl shadow-xl inline-block transform hover:scale-105 transition-all duration-300 hover:shadow-2xl animate-heartbeat">
        <p class="text-xl text-primary mb-4 font-semibold">¿Vas a remodelar tu hogar?</p>
        <h3 class="text-3xl font-bold text-white mb-6">¡Evita estos errores con asesoría profesional!</h3>
      </div>
    </div>
  </div>
</section>

<style>
  /* Animaciones personalizadas */
  @keyframes bulb {
    0%, 100% { transform: rotate(0deg); opacity: 1; }
    25% { transform: rotate(10deg); opacity: 0.8; }
    75% { transform: rotate(-10deg); opacity: 0.8; }
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  @keyframes tick {
    0% { transform: rotate(0deg); }
    25% { transform: rotate(15deg); }
    50% { transform: rotate(-15deg); }
    75% { transform: rotate(10deg); }
    100% { transform: rotate(0deg); }
  }

  @keyframes helmet {
    0% { transform: translateY(0); }
    50% { transform: translateY(-5px) rotate(5deg); }
    100% { transform: translateY(0); }
  }

  @keyframes cash {
    0% { transform: scale(1); }
    50% { transform: scale(1.1) rotate(5deg); }
    100% { transform: scale(1); }
  }

  .animate-bulb {
    animation: bulb 2s ease-in-out infinite;
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  .animate-tick {
    animation: tick 3s ease-in-out infinite;
  }

  .animate-helmet {
    animation: helmet 2.5s ease-in-out infinite;
  }

  .animate-cash {
    animation: cash 2s ease-in-out infinite;
  }


  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  [data-aos] {
    transition: all 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }
</style>