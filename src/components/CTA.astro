<section class="relative bg-gradient-to-br from-primary to-secondary text-white py-20 overflow-hidden">
  <!-- Partículas animadas de fondo -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
  </div>

  <div class="container mx-auto px-4 text-center relative z-10">
    <h2 class="text-5xl md:text-6xl font-extrabold mb-6 wow fadeInUp" 
        data-wow-duration="1s" 
        style="background: linear-gradient(120deg, #fff, #88F7FF, #fff);
               -webkit-background-clip: text;
               -webkit-text-fill-color: transparent;">
      ¿Listo para transformar tu espacio?
    </h2>
    
    <p class="text-xl md:text-2xl mb-12 wow fadeInUp" 
       data-wow-duration="1s" 
       data-wow-delay="0.2s">
      Contáctanos hoy y comienza tu proyecto de remodelación con expertos
    </p>

    <!-- Botones con efecto neón -->
    <div class="flex flex-wrap justify-center gap-4 md:gap-6 mb-12 wow fadeInUp" 
         data-wow-duration="1s" 
         data-wow-delay="0.4s">
         
      <a href="https://wa.me/573146755310" 
         target="_blank" 
         class="neon-button whatsapp">
        <i class="fab fa-whatsapp mr-3"></i> WhatsApp
        <div class="neon-border"></div>
      </a>
      
      <a href="mailto:ac.arteconstructivo@gmail.com" 
         class="neon-button email">
        <i class="fas fa-envelope mr-3"></i> Correo
        <div class="neon-border"></div>
      </a>
      
      <a href="https://www.facebook.com/ac.arquitecurayconstruccion" 
         target="_blank" 
         class="neon-button facebook">
        <i class="fab fa-facebook mr-3"></i> Facebook
        <div class="neon-border"></div>
      </a>
      
      <a href="https://www.instagram.com/ac_arquitecturayconstrucciones/" 
         target="_blank" 
         class="neon-button instagram">
        <i class="fab fa-instagram mr-3"></i> Instagram
        <div class="neon-border"></div>
      </a>
    </div>

    <!-- Botón principal con efecto holográfico -->
    <div class="relative mt-8">
      <div class="gooey-container">
        <a href="/contact" 
           class="holographic-button">
          <span>Contáctanos ahora</span>
          <div class="holographic-overlay"></div>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
/* Partículas animadas */
.particle {
  @apply absolute w-2 h-2 rounded-full bg-white/30;
  animation: float 5s linear infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-20px); }
}

.particle:nth-child(1) {
  top: 20%; left: 15%;
  animation-delay: 0s;
  animation-duration: 7s;
}

.particle:nth-child(2) {
  top: 40%; right: 10%;
  animation-delay: 1.5s;
  animation-duration: 6s;
}

.particle:nth-child(3) {
  bottom: 30%; left: 35%;
  animation-delay: 3s;
  animation-duration: 8s;
}

/* Botones con efecto neón */
.neon-button {
  @apply relative inline-flex items-center px-6 py-3 font-bold rounded-full 
         transition-all duration-300;
  text-shadow: 0 0 10px rgba(255,255,255,0.3);
}

.neon-button.whatsapp {
  background: linear-gradient(135deg, #25D366, #128C7E);
  box-shadow: 0 0 20px #25D366;
}

.neon-button.email {
  background: linear-gradient(135deg, #BB001B, #8C1D40);
  box-shadow: 0 0 20px #BB001B;
}

.neon-button.facebook {
  background: linear-gradient(135deg, #4267B2, #1877F2);
  box-shadow: 0 0 20px #4267B2;
}

.neon-button.instagram {
  background: linear-gradient(45deg, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%);
  box-shadow: 0 0 20px #e1306c;
}

.neon-button:hover {
  transform: scale(1.05) rotate(-2deg);
  box-shadow: 0 0 30px rgba(255,255,255,0.5);
}

.neon-border {
  @apply absolute inset-0 rounded-full;
  border: 2px solid;
  opacity: 0.5;
  animation: pulse-border 1.5s ease-out infinite;
}

@keyframes pulse-border {
  0% {
    transform: scale(1);
    opacity: 0.5;
  }
  50% {
    transform: scale(1.2);
    opacity: 0;
  }
  100% {
    transform: scale(1);
    opacity: 0.5;
  }
}

/* Botón holográfico */
.gooey-container {
  filter: url('#goo');
  isolation: isolate;
}

.holographic-button {
  @apply relative inline-block px-8 py-4 font-extrabold rounded-full overflow-hidden;
  background: linear-gradient(120deg, #b84c0e, #da680c, #b35e37);
  background-size: 200% 200%;
  transition: 0.5s;
  z-index: 1;
}

.holographic-overlay {
  @apply absolute inset-0 bg-white/10;
  transform: translateX(-100%);
  transition: transform 0.5s ease-out;
}

.holographic-button:hover .holographic-overlay {
  transform: translateX(100%);
}

.holographic-button:hover {
  animation: hologram 1.5s infinite;
}

@keyframes hologram {
  0% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 50%;
  }
}

.holographic-button span {
  @apply relative z-10;
  color: #ffffff;
  text-shadow: 0 0 10px rgba(0, 247, 255, 0.5);
}

/* Efectos adicionales */
@keyframes gradient-pulse {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

h2 {
  animation: gradient-pulse 5s linear infinite;
}
</style>