
<section id="home" class="relative min-h-screen flex items-center justify-center">
  <!-- Background image with overlay -->
  <div class="absolute inset-0 z-0">
    <img 
      src="/banner.webp" 
      alt="Modern interior design" 
      class="w-full h-full object-cover"
      width="1920"  
      height="1080"
    />
    <div class="absolute inset-0 bg-black/50"></div>
  </div>

  <!-- Content -->
  <div class="container mx-auto px-4 relative z-10 text-center flex flex-col items-center justify-center">
    <div class="max-w-3xl" data-aos="fade-up">
      <!-- Company Name (H1) -->
      <h1 class="text-3xl md:text-5xl lg:text-6xl font-extrabold text-white mb-4">
        <span class="block">AC Arquitectura & Construcción SAS</span>
        <span class="block text-2xl md:text-3xl font-light text-gray-200 mt-2">
          Transformamos tus espacios con diseño y calidad
        </span>
      </h1>

      <!-- Main Tagline -->
      <h2 class="text-xl md:text-2xl font-medium text-gray-200 mb-8">
        Diseñamos tus espacios, construimos tus sueños.
      </h2>

      <!-- Call-to-action buttons -->
      <div class="flex flex-col sm:flex-row justify-center gap-4 w-full">
        <a 
          href="#servicios" 
          class="btn w-full sm:w-auto mx-auto sm:mx-0"
        >
          <svg class="absolute top-0 left-0 w-full h-full" viewBox="0 0 277 62">
            <defs>
              <linearGradient id="grad1">
                <stop offset="0%" stop-color="#F27405"/>
                <stop offset="100%" stop-color="#F27405" />
              </linearGradient>
            </defs>
            <rect x="5" y="5" rx="25" fill="none" stroke="url(#grad1)" width="266" height="50"></rect>
          </svg>
          <span>Nuestros Servicios</span>
        </a>
        <a 
          href="/contact" 
          class="btn w-full sm:w-auto mx-auto sm:mx-0"
        >
          <svg class="absolute top-0 left-0 w-full h-full" viewBox="0 0 277 62">
            <defs>
              <linearGradient id="grad1">
                <stop offset="0%" stop-color="#F27405"/>
                <stop offset="100%" stop-color="#F27405" />
              </linearGradient>
            </defs>
            <rect x="5" y="5" rx="25" fill="none" stroke="url(#grad1)" width="266" height="50"></rect>
          </svg>
          <span>Contáctanos</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  /* Responsive container */
  .container {
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  /* Buttons */
  .btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 277px; /* Ancho base para desktop */
    height: 50px;
    font-size: 1em;
    font-weight: bold;
    text-align: center;
    text-transform: uppercase;
    background-color: transparent;
    cursor: pointer;
    text-decoration: none;
    font-family: 'Roboto', sans-serif;
    font-weight: 900;
    font-size: 17px;
    letter-spacing: 0.045em;
    color: black; /* Cambia el color del texto a negro */
    overflow: hidden; /* Asegura que el SVG no se desborde */
  }

  .btn svg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%; /* Asegura que el SVG ocupe todo el ancho del botón */
    height: 100%; /* Asegura que el SVG ocupe todo el alto del botón */
    pointer-events: none; /* Evita que el SVG interfiera con los clics */
  }

  .btn svg rect {
    stroke-width: 4;
    stroke-dasharray: 353, 0;
    stroke-dashoffset: 0;
    transition: all 600ms ease;
  }

  .btn span {
    position: relative;
    z-index: 1; /* Asegura que el texto esté sobre el SVG */
    background: none; /* Elimina el gradiente de fondo */
    color: rgb(255, 255, 255); /* Cambia el color del texto a negro */
  }

  .btn:hover svg rect {
    stroke-width: 4;
    stroke-dasharray: 196, 543;
    stroke-dashoffset: 437;
  }

  /* Adjustments for mobile */
  .flex.flex-col > .btn {
    margin-left: auto;
    margin-right: auto;
    width: 100%; /* Botones ocupan todo el ancho en móviles */
  }

  .flex.sm\:flex-row.justify-center > .btn {
    margin-left: 0;
    margin-right: 0;
    width: auto; /* Restaura el ancho original en desktop */
  }
</style>