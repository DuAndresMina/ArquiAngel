---
import { Image } from 'astro:assets';
---

<section id="home" class="relative min-h-screen flex items-center pt-16">
  <!-- Background image with overlay -->
  <div class="absolute inset-0 z-0">
    <img 
      src="/banner.webp" 
      alt="Modern interior design" 
      class="w-full h-full object-cover"
      loading="lazy"
      width="1920"  <!-- Especifica el ancho de la imagen -->
      height="1080" <!-- Especifica la altura de la imagen -->
    />
    <div class="absolute inset-0 bg-black/50"></div>
  </div>

  <!-- Content -->
  <div class="container mx-auto px-4 relative z-10">
    <div class="max-w-3xl" data-aos="fade-up">
      <h1 class="text-4xl md:text-6xl font-bold text-white mb-6">
        Diseñamos tus espacios, construimos tus sueños.
      </h1>
      <p class="text-xl text-gray-200 mb-8">
        Servicios profesionales de remodelación de casas, apartamentos y espacios comerciales.
      </p>
      <div class="flex flex-wrap gap-4">
        <a 
          href="#services" 
          class="bg-secondary hover:bg-secondary/90 text-white px-8 py-3 rounded-lg transition-colors"
        >
          Nuestros Servicios
        </a>
        <a 
          href="/contact" 
          class="bg-white hover:bg-gray-100 text-primary px-8 py-3 rounded-lg transition-colors"
        >
          Contáctanos
        </a>
      </div>
    </div>
  </div>
</section>