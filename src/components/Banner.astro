<div class="bg-primary py-20 relative overflow-hidden">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Partículas de fondo animadas -->
      <div class="absolute inset-0 z-0">
        <div class="absolute w-2 h-2 bg-white rounded-full animate-particle" 
             style="top:20%; left:15%"></div>
        <div class="absolute w-3 h-3 bg-secondary rounded-full animate-particle-delayed" 
             style="top:40%; right:10%"></div>
        <div class="absolute w-2 h-2 bg-accent rounded-full animate-particle" 
             style="top:70%; left:30%"></div>
      </div>
  
      <div class="text-center mb-25 relative z-10" >
        <h2 class="text-4xl md:text-5xl font-bold mb-6 text-white transform hover:scale-105 transition-all duration-300">
          <span class="animated-gradient-text">¿QUÉ ESPERAS PARA DAR</span><br>
          <span class="animated-gradient-text delay-100">EL PRIMER PASO PARA</span><br>
          <span class="animated-gradient-text delay-200">REMODELAR TU VIVIENDA?</span>
        </h2>
        
        <div class="h-1 w-24 bg-white mx-auto mt-6 animate-line-expand"></div>
      </div>
  
      <!-- Elementos flotantes -->
      <div class="absolute top-0 left-0 w-full h-full pointer-events-none">
        <div class="absolute top-20 left-1/4 w-16 h-16 bg-white/10 rounded-full animate-float-1"></div>
        <div class="absolute top-40 right-20 w-24 h-24 bg-secondary/20 rounded-full animate-float-2"></div>
      </div>
    </div>

    </div>

    
<style>
    /* Animaciones personalizadas */
    @keyframes gradient-pulse {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }
  
    @keyframes line-expand {
      0% { transform: scaleX(0); opacity: 0; }
      100% { transform: scaleX(1); opacity: 1; }
    }
  
    @keyframes particle-float {
      0% { transform: translateY(0) rotate(0deg); opacity: 0.8; }
      100% { transform: translateY(-100vh) rotate(360deg); opacity: 0; }
    }
  
    @keyframes float {
      0%, 100% { transform: translateY(0) scale(1); }
      50% { transform: translateY(-20px) scale(1.05); }
    }
  
    .animated-gradient-text {
      background: linear-gradient(
        45deg,
        #FFFFFF 25%,
        #F27405 50%,
        #FFFFFF 75%
      );
      background-size: 400% 400%;
      background-clip: text;
      -webkit-background-clip: text;
      color: transparent;
      animation: gradient-pulse 6s ease infinite;
    }
  
    .animated-gradient-text.delay-100 {
      animation-delay: 0.5s;
    }
  
    .animated-gradient-text.delay-200 {
      animation-delay: 1s;
    }
  
    .animate-line-expand {
      animation: line-expand 1s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
    }
  
    .animate-particle {
      animation: particle-float 8s linear infinite;
    }
  
    .animate-particle-delayed {
      animation: particle-float 10s linear infinite 2s;
    }
  
    .animate-float-1 {
      animation: float 6s ease-in-out infinite;
    }
  
    .animate-float-2 {
      animation: float 5s ease-in-out infinite 1s;
    }
  
    [data-aos="zoom-in"] {
      transform: scale(0.95);
      opacity: 0;
      transition: all 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }
  
    [data-aos="zoom-in"].aos-animate {
      transform: scale(1);
      opacity: 1;
    }
  </style>